#pragma once

#include "../header.h"

namespace Tests {
    /* Tests to be added:
     * - Drone should not change position when in hover
     * - Drone should only move upwards when full throttle applied to all thrusters
     * - Drone should pitch (not roll or yaw) when right throttle > left throttle
     * - Same ideas for roll and yaw
     */
};